<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Financial Manager</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: Arial, sans-serif;
      background: url('/images/dark-finance-bg.jpg') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }

    body {
      display: flex;
      flex-direction: column;
    }

    .container {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }

    .hero-title {
      font-size: 48px;
      font-weight: bold;
      text-shadow: 2px 2px 15px rgba(0,0,0,0.7);
      margin-bottom: 10px;
    }

    .hero-quote {
      font-size: 26px;
      font-weight: 500;
      margin-bottom: 30px;
      max-width: 800px;
      text-shadow: 2px 2px 15px rgba(0,0,0,0.7);
      cursor: pointer;
      border-right: .15em solid #36A2EB;
      white-space: nowrap;
      overflow: hidden;
      width: 0;
      animation: typing 3s steps(60, end) forwards, blink 0.75s step-end infinite;
    }

    @keyframes typing { from { width: 0 } to { width: 100% } }
    @keyframes blink { 50% { border-color: transparent } }

    .hero-buttons {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      justify-content: center;
    }

    @keyframes slideInLeft {
      from { transform: translateX(-100%); opacity: 0; }
      to { transform: translateX(0); opacity: 1; }
    }

    .hero-buttons a {
      background-color: #36A2EB;
      color: white;
      text-decoration: none;
      padding: 15px 30px;
      border-radius: 8px;
      font-size: 18px;
      font-weight: bold;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
    }

    /* Delay buttons to appear after quote typing (3s) */
    .hero-buttons a:nth-child(1) { animation: slideInLeft 1s forwards 3.2s; }
    .hero-buttons a:nth-child(2) { animation: slideInLeft 1s forwards 3.7s; }
    .hero-buttons a:nth-child(3) { animation: slideInLeft 1s forwards 4.0s; }

    .google-btn {
      background-color: #DB4437;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .google-btn i { font-size: 20px; }

    footer {
      height: 60px;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: rgba(0,0,0,0.8);
      font-size: 14px;
      color: #ccc;
    }

    /* ✅ Responsive Design */
    @media (max-width: 1024px) {
      .hero-title {
        font-size: 40px;
      }
      .hero-quote {
        font-size: 22px;
        max-width: 90%;
      }
      .hero-buttons a {
        font-size: 16px;
        padding: 12px 25px;
      }
    }

    @media (max-width: 768px) {
      .container {
        padding: 15px;
      }
      .hero-title {
        font-size: 32px;
        text-align: center;
        line-height: 1.3;
      }
      .hero-quote {
        font-size: 18px;
        text-align: center;
        white-space: normal;
        border: none;
        animation: none;
        width: 100%;
      }
      .hero-buttons {
        flex-direction: column;
        gap: 15px;
        width: 100%;
      }
      .hero-buttons a {
        width: 80%;
        margin: 0 auto;
        justify-content: center;
      }
    }

    @media (max-width: 480px) {
      .hero-title {
        font-size: 26px;
      }
      .hero-quote {
        font-size: 16px;
      }
      .hero-buttons a {
        font-size: 14px;
        padding: 10px 20px;
        width: 90%;
      }
      .google-btn i {
        font-size: 16px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="hero-title">Welcome to Financial Manager</div>
    <p class="hero-quote">'Manage your money wisely, grow your wealth, and achieve freedom.'</p>

    <div class="hero-buttons">
      <a href="/login">Login</a>
      <a href="/register">Register</a>
      <a href="/auth/google" class="google-btn"><i class="fab fa-google"></i> Sign in with Google</a>
    </div>
  </div>

  <footer>
    &copy; 2025 Financial Management System
  </footer>
</body>
</html>
